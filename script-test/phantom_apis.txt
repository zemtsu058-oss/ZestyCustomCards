# Danh sách "API ma" — các hàm KHÔNG tồn tại trong EDOPro nhưng từng bị
# AI/agent bịa ra khi viết script, gây crash runtime "attempt to call a nil value".
# Validator (validate_scripts.ps1) sẽ báo FAIL nếu bất kỳ script nào gọi các hàm này.
#
# Quy tắc bổ sung: mỗi khi gặp lỗi runtime "attempt to call a nil value (field 'X')"
# do hàm không tồn tại, thêm 1 dòng "Namespace.X" hoặc ":X(" vào file này
# để chặn vĩnh viễn lỗi tái diễn.
#
# Phiên 062: hàm cost bịa, không có trong EDOPro
Cost.DetachFromSelf
Card.GetRelatedHandler
